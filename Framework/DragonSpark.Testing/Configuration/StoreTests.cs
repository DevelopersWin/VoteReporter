using System.Reflection;
using DragonSpark.Configuration;
using DragonSpark.Diagnostics;
using DragonSpark.Testing.Framework;
using DragonSpark.Testing.Framework.Setup;
using Serilog.Events;
using Xunit;

namespace DragonSpark.Testing.Configuration
{
	public class StoreTests
	{
		/*[Fact, MinimumLevel( LogEventLevel.Debug )]
		public void LoadWorksAsExpected()
		{
			using ( MethodBase.GetCurrentMethod().AsCurrentContext() )
			{
				var load = Load<ProfilerLevelConfiguration, LogEventLevel>.Get();
				A
			}
		}*/

		public void AnotherMethod() {}
	}
}